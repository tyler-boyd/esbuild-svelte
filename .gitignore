# duh
**node_modules/

#should be built by running `node build.js` fresh every time
# Need these in the repo to use it as a git source in npm
# dist/index.mjs
# dist/index.js

# from example
example/dist/
example/package-lock.json
